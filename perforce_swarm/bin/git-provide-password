#!/usr/bin/env ruby

require_relative '../init'
require_relative '../config'
require_relative '../git_fusion'

begin
  url = PerforceSwarm::GitFusion::URL.new(id: ARGV[0])
  puts url.password
rescue
  # TODO: do we want to log or do anything extra here if there is an error?
  puts ''
end
